package ${package}.rest;

import com.cetc.common.web.controller.AbstractSpringController;
import ${package}.service.${className}Service;
import ${package}.entity.${className};
import org.springframework.web.bind.annotation.RequestMapping;
import io.swagger.annotations.Api;
/**
 * ${comments}
 *
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
@Api(value = "/${secondModuleName}", tags = "${comments}接口模块")
@RestController
@RequestMapping("${secondModuleName}")
public class ${className}Controller extends AbstractSpringController<${className},${className}Service> {

}